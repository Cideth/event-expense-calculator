// // 해당 컴포넌트에서 할 일

// 참석자의 상태(관리자 여부, 현재 참석 여부)를 포함한 참석자 목록과 초대 링크를 복사 할 수 있는 버튼 컴포넌트

export function InviteesList() {
  return <div>InviteesList</div>;
}

export function InviteButton() {
  return <div>InviteButton</div>;
}
